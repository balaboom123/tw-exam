name: sync-ceec-ast

on:
  workflow_dispatch:
  schedule:
    - cron: "30 3 * * 6"

permissions:
  contents: write

jobs:
  sync:
    uses: ./.github/workflows/_sync-provider.yml
    with:
      provider_id: ceec_ast
      cache_prefix: ceec-ast
      timeout_minutes: 120
      publish: true
