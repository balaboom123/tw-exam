name: sync-taisugar-recruit

on:
  # Keep this manual and provider-only until its historical migration is reviewed.
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    uses: ./.github/workflows/_sync-provider.yml
    with:
      provider_id: taisugar_recruit
      cache_prefix: taisugar-recruit
      timeout_minutes: 120
      publish: false
