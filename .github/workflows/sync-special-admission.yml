name: sync-special-admission

on:
  workflow_dispatch:
  schedule:
    - cron: "50 3 * * 6"

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    concurrency:
      group: special-admission
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Restore mirror directory
        uses: actions/cache/restore@v6
        with:
          path: mirror
          key: special-admission-mirror-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            special-admission-mirror-

      - name: Run special admission sync
        run: python -m app sync-full --provider special_admission --site-id default

      - name: Save mirror directory
        if: '!cancelled()'
        uses: actions/cache/save@v6
        with:
          path: mirror
          key: special-admission-mirror-${{ github.run_id }}-${{ github.run_attempt }}

      - name: Commit regenerated special admission provider data
        # sync-full writes the papers it did fetch and records the ones it could
        # not, then exits non-zero. Gating this step on success discarded the
        # whole refresh whenever one upstream file was unavailable, and kept the
        # failure record out of the repository where it could be acted on.
        if: '!cancelled()'
        run: >-
          bash .github/scripts/commit-and-push.sh "chore: refresh special admission provider data" data/providers/special_admission
