name: sync-teacher-recruit-central-alliance

on:
  # Keep this manual while the official source is expired.
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    uses: ./.github/workflows/_sync-provider.yml
    with:
      provider_id: teacher_recruit_central_alliance
      cache_prefix: teacher-recruit-central-alliance
      timeout_minutes: 30
      publish: false
